import { render } from "@testing-library/react";
import LimitAll from "../../components/limit/LimitAll";
test("should first", () => {
  expect(true).toBe.true;
});

test("hey", () => {
  render(<LimitAll />);
});
